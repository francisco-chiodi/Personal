spring.application.name=web
# PostgreSQL Database Connection Layout
spring.datasource.url=jdbc:postgresql://localhost:5432/lain_db
spring.datasource.username=lain
spring.datasource.password=_Ikawura12#

# JPA / Hibernate Settings
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Automatically creates or updates tables based on your @Entity classes
spring.jpa.hibernate.ddl-auto=update

# Shows the exact SQL statements in your console (great for debugging tests)
spring.jpa.show-sql=true